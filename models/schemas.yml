version: 2

models:
  - name: fact_order
    columns:
      - name: salesorderdetailid

  - name: dim_client
    columns:
      - name: client_sk

  - name: dim_region
    columns:
      - name: region_sk
        
  - name: dim_products
    columns:
      - name: product_sk